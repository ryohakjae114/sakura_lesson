= title_dom(t('.title'))
= simple_form_for [:admins, @lesson] do |f|
  = f.input :title
  = f.input :summary
  = f.input :instructor
  = f.input :published
  = f.button :submit

= button_to t('.delete'), admins_lesson_path(@lesson),
            class: 'btn btn-danger mt-5',
            method: :delete, data: { turbo_confirm: t('.delete_confirm') }
